# Web client config — copy to .env.local. Never commit .env.local.
# Must match the gateway's JWT_SECRET so minted dev tokens validate.
JWT_SECRET=replace-with-the-same-secret-as-the-gateway
# Gateway WebSocket endpoint reachable from the browser.
NEXT_PUBLIC_GATEWAY_WS=ws://localhost:8080/ws
